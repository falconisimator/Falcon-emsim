# Python build / cache artifacts
__pycache__/
*.pyc
build/
dist/
*.egg-info/
.pytest_cache/

# Validation output images (regenerated by tests) and stray exports
validation/figures/
*.gif

# Keep the web wheel (docs/wheels/*.whl) — it is a deploy artifact served by Pages.
